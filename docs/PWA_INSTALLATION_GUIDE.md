# PWA Installation Guide

## Преглед

BodyBest е напълно функционално Progressive Web App (PWA), което може да бъде инсталирано на всички съвременни браузъри и устройства.

## Какво е PWA?

Progressive Web App (PWA) е уеб приложение, което може да бъде инсталирано на вашето устройство и да работи като нативно приложение, включително:

- ✅ **Offline работа** - Функционира без интернет връзка
- ✅ **Инсталация на Home Screen** - Икона на началния екран като обикновено приложение
- ✅ **Бързо зареждане** - Кеширани ресурси за моментално зареждане
- ✅ **Push нотификации** - Получавайте уведомления (бъдеща функция)
- ✅ **Автоматични обновления** - Винаги имате най-новата версия
- ✅ **Няма App Store** - Инсталирате директно от браузъра

## Как да инсталирам?

### Android (Chrome, Edge, Samsung Internet)

#### Метод 1: Автоматичен prompt (Препоръчително)

1. Отворете https://radilovk.github.io/bodybest в Chrome
2. След 3 секунди ще се появи banner "Добавяне към началния екран"
3. Натиснете **"Добави"** или **"Install"**
4. Потвърдете инсталацията
5. Готово! Приложението е на Home Screen

#### Метод 2: Бутон за инсталация

1. Отворете сайта
2. Потърсете плаващия син бутон с икона 📥 в долния десен ъгъл
3. Натиснете бутона "Инсталирай приложението"
4. Потвърдете
5. Готово!

#### Метод 3: През менюто на браузъра

1. Отворете сайта в Chrome
2. Натиснете меню (три точки) ⋮
3. Изберете **"Добави към началния екран"** или **"Install app"**
4. Потвърдете
5. Готово!

### iOS (Safari)

⚠️ **Забележка:** iOS не поддържа автоматични install prompts, но можете да добавите към Home Screen:

1. Отворете сайта в Safari
2. Натиснете бутона Share (квадрат със стрелка нагоре)
3. Превъртете надолу и изберете **"Add to Home Screen"**
4. Задайте име (напр. "ONE BODY")
5. Натиснете **"Add"**
6. Готово!

**iOS Limitations:**
- Няма автоматичен install prompt
- Работи само в Safari
- Ограничена поддръжка на service workers

### Desktop (Chrome, Edge)

1. Отворете сайта
2. В адресната лента вдясно ще видите икона за инсталация ⊕
3. Натиснете иконата
4. Изберете **"Install"**
5. Приложението ще се отвори в отделен прозорец
6. Готово!

**Алтернативно:**
- Chrome menu → More tools → Create shortcut → ✓ Open as window
- Edge menu → Apps → Install this site as an app

## Функции на PWA

### 1. Offline работа

Приложението работи offline благодарение на Service Worker:

- **Кеширани страници**: index.html, code.html, quest.html, assistant.html
- **Кеширани стилове**: Всички CSS файлове
- **Кеширани скриптове**: Основни JS модули
- **Offline logging**: Записите се пазят локално и се синхронизират при връзка

#### Offline Indicator

Когато сте offline:
- Червена линия в горната част на екрана
- Всички записи се пазят в localStorage
- Автоматична синхронизация при връзка

### 2. Background Sync

При възстановяване на връзката:
- Автоматична синхронизация на pending logs
- Изпращане на offline записи към сървъра
- Актуализиране на кеширани данни

### 3. Автоматични обновления

Service Worker автоматично:
- Проверява за нови версии
- Показва notification за обновление
- Актуализира приложението с един клик

### 4. Install Prompt Management

#### Автоматично показване

Prompt-ът се показва автоматично при:
- Първо посещение на сайта
- След 3 секунди след зареждане
- Само ако не е инсталирано вече

#### Забрана на повторно показване

Ако потребителят откаже инсталацията:
- Prompt-ът няма да се показва отново 7 дни
- Може ръчно да инсталира от бутона

#### Custom Install Button

Винаги достъпен бутон за инсталация:
- Плаващ бутон в долен десен ъгъл
- Показва се само ако приложението не е инсталирано
- Може да се използва по всяко време

## Технически детайли

### Manifest.json

Конфигурационен файл с метаданни за приложението:

```json
{
  "name": "ONE BODY - Вашата Индивидуална Здравна Система",
  "short_name": "ONE BODY",
  "start_url": "/",
  "display": "standalone",
  "theme_color": "#007bff",
  "background_color": "#f4f7f6",
  "icons": [...]
}
```

**Полета:**
- `name`: Пълно име (до 45 символа)
- `short_name`: Кратко име за Home Screen
- `start_url`: URL при стартиране
- `display`: Режим на показване (standalone = без browser UI)
- `theme_color`: Цвят на header bar
- `background_color`: Цвят при стартиране
- `icons`: Икони за различни резолюции

### Service Worker

JavaScript файл, който работи на заден план:

**Функции:**
- Кеширане на ресурси
- Offline functionality
- Background sync
- Push notifications (бъдеща функция)

**Стратегии за кеширане:**

1. **Cache First** (Static assets)
   - Проверява кеша първо
   - Fallback към network
   - Използва се за CSS, JS, изображения

2. **Network First** (API requests)
   - Проверява network първо
   - Fallback към кеш
   - Използва се за /api/* заявки

### PWA Install Module

JavaScript модул за управление на инсталацията:

**Функции:**
- `registerServiceWorker()` - Регистрира SW
- `setupInstallPrompt()` - Setup на prompt handling
- `showInstallPrompt()` - Показва install диалог
- `checkIfInstalled()` - Проверява дали е инсталирано

**События:**
- `beforeinstallprompt` - Браузърът иска да покаже prompt
- `appinstalled` - Приложението е инсталирано
- `online/offline` - Промяна на connection status

## Тестване

### Локално тестване

```bash
# 1. Start dev server
npm run dev

# 2. Отвори в Chrome
# http://localhost:5173

# 3. Отвори DevTools
# Application tab → Manifest
# Application tab → Service Workers
# Application tab → Storage

# 4. Test offline
# Network tab → Offline checkbox
```

### Production тестване

```bash
# 1. Build
npm run build

# 2. Serve production build
npx serve -s dist -p 8080

# 3. Test на Android Chrome
# Chrome → DevTools → Remote devices
# Inspect → Lighthouse → PWA audit
```

### PWA Checklist

Използвай Google Lighthouse за audit:

```bash
# Chrome DevTools → Lighthouse
# ✓ Progressive Web App
# ✓ Performance
# ✓ Accessibility
# ✓ Best Practices
```

**Критерии за PWA:**
- ✅ Manifest.json с правилна структура
- ✅ Service worker регистриран
- ✅ HTTPS (или localhost)
- ✅ Responsive design
- ✅ Icons в правилни размери
- ✅ Start URL работи offline
- ✅ Theme color set

## Troubleshooting

### Приложението не се инсталира

**Решения:**
1. Проверете дали използвате HTTPS
2. Изчистете кеша на браузъра
3. Проверете DevTools Console за грешки
4. Проверете Service Worker status в DevTools

### Offline режим не работи

**Решения:**
1. Проверете дали SW е активен (DevTools → Application)
2. Изчистете кеша и презаредете
3. Проверете Network tab → Disable cache е изключен
4. Рестартирайте браузъра

### Икони не се показват правилно

**Решения:**
1. Проверете manifest.json пътищата
2. Икони трябва да са в правилните размери
3. Изчистете кеша
4. Деинсталирайте и инсталирайте отново

### Install prompt не се появява

**Причини:**
- Приложението вече е инсталирано
- Prompt е отказан преди < 7 дни
- Browser не поддържа beforeinstallprompt
- PWA критериите не са изпълнени

**Решения:**
1. Използвайте ръчния бутон за инсталация
2. Проверете Lighthouse audit
3. Изчакайте 7 дни или изчистете browser data

### Update не се прилага

**Решения:**
1. Натиснете бутона "Актуализирай" в notification
2. Затворете всички tabs на приложението
3. Изчистете кеша
4. Рестартирайте браузъра

## Допълнителни ресурси

### Документация
- [MDN PWA Guide](https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps)
- [Google PWA Checklist](https://web.dev/pwa-checklist/)
- [Service Worker API](https://developer.mozilla.org/en-US/docs/Web/API/Service_Worker_API)

### Инструменти
- [Lighthouse](https://developers.google.com/web/tools/lighthouse)
- [PWA Builder](https://www.pwabuilder.com/)
- [Manifest Generator](https://app-manifest.firebaseapp.com/)

### Browser Support
- ✅ Chrome 40+ (Full support)
- ✅ Edge 17+ (Full support)
- ✅ Firefox 44+ (Partial support)
- ⚠️ Safari 11.1+ (Limited support)
- ✅ Samsung Internet 4+ (Full support)

## Поддръжка

За проблеми и въпроси:
- GitHub Issues: https://github.com/Radilovk/bodybest/issues
- Email: support@bodybest.com

---

**Версия:** 1.0.0  
**Последна актуализация:** 2026-01-03
